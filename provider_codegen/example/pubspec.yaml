name: example
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  provider_annotation:
    path: ../provider_annotation

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner:
  provider_codegen:
    path: ../provider_codegen

flutter:
  uses-material-design: true
