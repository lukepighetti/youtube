name: example
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  state_annotation:
    path: ../state_annotation

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner:
  state_codegen:
    path: ../state_codegen

flutter:
  uses-material-design: true
